{% extends 'base.html.twig' %}

{% block title %}Contacto{% endblock %}

{% block navbaritems %}
    <li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Principal <span class="sr-only">(current)</span></a></li>
    <li class="nav-item"><a class="nav-link" href="{{ path('post_index') }}">Blog</a></li>
    <li class="nav-item"><a class="nav-link active" href="{{ path('contact_new') }}">Contacto</a></li>
{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-8">
            <div class="card">
                <div class="card-header border-0">
                    <h2 class="mb-0 text-center">Contacto</h2>
                </div>
                <div class="card-body">
                    {{ include('@AplycaBlog/contact/_form.html.twig', {'button_label': 'Enviar'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
