{{ form_start(form) }}
    <div class="row">
        <div class="col-9">
            <div class="form-group">
                {{ form_widget(form.title, {'attr': {'placeholder': 'Título'}}) }}
            </div>
        </div>
        <div class="col-3">
            <div class="form-group">
                {{ form_widget(form.image, {'attr': {'placeholder': 'Url Imagen'}}) }}
            </div>
        </div>
        <div class="col-12">
            <div class="form-group">
                {{ form_widget(form.content, {'attr': {'placeholder': 'Contenido'}}) }}
            </div>
        </div>
        <div class="col-12 d-flex justify-content-center">
            <a href="{{ path('post_index') }}" class="btn btn-outline-default">Volver</a>
            <button class="btn btn-success">{{ button_label|default('Almancenar') }}</button>
        </div>
    </div>
{{ form_end(form) }}
